import MobileExperience from "@/features/farm/components/mobile/MobileExperience";

// OJAN.FARM v2.0 — the phone-OS farm portfolio. Shares data/state/art with the
// desktop valley (ojantigakali web repo). The old arcade game is retained in
// src/components for rollback.
export default function Page() {
  return <MobileExperience />;
}
